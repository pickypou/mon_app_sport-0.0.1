import 'package:flutter/material.dart';

import '../../../theme/theme_styles.dart';

Widget titleHome = Container(
  margin: const EdgeInsets.fromLTRB(0, 60, 0, 30),
  child:
  Text('Unipampa Push'.toUpperCase(),
      style: titleStyle
      ),
);